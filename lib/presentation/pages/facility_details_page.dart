import 'package:flutter/material.dart';

class FacilityDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Facility Details')),
      body: Center(
        child: Text('Facility Details Page'),
      ),
    );
  }
}
